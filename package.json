{
    "name": "croppulse",
    "version": "1.0.0",
    "scripts": {
        "start": "concurrently \"npm run client\" \"npm run server\"",
        "client": "cd client && npm start",
        "server": "run-flask.bat"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    },
    "dependencies": {
        "jwt-decode": "^4.0.0",
        "react-toastify": "^11.0.5"
    }
}
